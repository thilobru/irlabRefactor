docker rmi $(docker images -q)

docker-compose up -d

sudo chown -R 1000:1000 tira/ES

python
>>> import nltk
>>> nltk.download('wordnet')
>>> nltk.download('omw-1.4')

docker container ls

watch docker container ls

docker exec -it irlab_python_1 bash
python tira/2_run.py -i i -o tira